name: utils-update

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  update:
    uses: sapphire-sh/github-actions/.github/workflows/utils-update-template.yml@main
    permissions:
      contents: write
      pull-requests: write
